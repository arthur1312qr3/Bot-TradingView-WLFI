name: Keep Bot Alive 24/7

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    
    steps:
      - name: Ping Bot
        run: |
          curl -s https://bot-tradingview-wlfi.onrender.com/health
          sleep 30
          curl -s https://bot-tradingview-wlfi.onrender.com/
      
      - name: Log
        run: echo "✅ Ping em $(date)"
